{
  "workflows": [
    {
      "id": "feedback.quick.submit",
      "name": "Quick Feedback",
      "description": "Quick feedback submission",
      "steps": [
        { "id": "1", "action": "ui.show.feedback.form" },
        { "id": "2", "action": "feedback.create" },
        { "id": "3", "action": "notify.success" }
      ],
      "triggers": ["menu.feedback.send"]
    },
    {
      "id": "feedback.rate.feature",
      "name": "Rate Feature",
      "description": "Rate a feature",
      "steps": [
        { "id": "1", "action": "ui.show.rating" },
        { "id": "2", "action": "rating.submit" },
        { "id": "3", "action": "notify.thanks" }
      ],
      "triggers": ["menu.feedback.rate"]
    }
  ]
}
