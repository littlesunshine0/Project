{
  "workflows": [
    {
      "id": "content.sync.all",
      "name": "Sync All Content",
      "description": "Sync content from all sources",
      "steps": [
        { "id": "1", "action": "source.list" },
        { "id": "2", "action": "content.sync" },
        { "id": "3", "action": "notify.complete" }
      ],
      "triggers": ["schedule.hourly", "menu.content.sync"]
    }
  ]
}
