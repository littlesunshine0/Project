{
  "workflows": [
    {
      "id": "syntax.auto.format",
      "name": "Auto Format",
      "description": "Auto format code",
      "steps": [
        { "id": "1", "action": "syntax.parse" },
        { "id": "2", "action": "format.code" },
        { "id": "3", "action": "syntax.highlight" }
      ],
      "triggers": ["shortcut.⇧⌥F"]
    }
  ]
}
