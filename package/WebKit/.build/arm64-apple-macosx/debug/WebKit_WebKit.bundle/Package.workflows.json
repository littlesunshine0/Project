{
  "workflows": [
    {
      "id": "web.preview.url",
      "name": "Preview URL",
      "description": "Preview a web URL",
      "steps": [
        { "id": "1", "action": "web.load" },
        { "id": "2", "action": "ui.show.preview" }
      ],
      "triggers": ["menu.web.preview"]
    }
  ]
}
