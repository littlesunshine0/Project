{
  "workflows": [
    {
      "id": "marketplace.install.flow",
      "name": "Install Package",
      "description": "Install a package from marketplace",
      "steps": [
        { "id": "1", "action": "package.browse" },
        { "id": "2", "action": "ui.select.single" },
        { "id": "3", "action": "package.install" },
        { "id": "4", "action": "notify.complete" }
      ],
      "triggers": ["menu.marketplace.install"]
    },
    {
      "id": "marketplace.update.all",
      "name": "Update All",
      "description": "Update all packages",
      "steps": [
        { "id": "1", "action": "check.updates" },
        { "id": "2", "action": "package.update.batch" },
        { "id": "3", "action": "notify.complete" }
      ],
      "triggers": ["menu.marketplace.update.all"]
    }
  ]
}
