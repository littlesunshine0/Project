{
  "workflows": [
    {
      "id": "asset.import.flow",
      "name": "Import Asset",
      "description": "Import and process asset",
      "steps": [
        { "id": "1", "action": "ui.show.file.picker" },
        { "id": "2", "action": "asset.import" },
        { "id": "3", "action": "asset.optimize" },
        { "id": "4", "action": "notify.complete" }
      ],
      "triggers": ["menu.asset.import"]
    }
  ]
}
