{
  "workflows": [
    {
      "id": "icon.pick",
      "name": "Pick Icon",
      "description": "Pick an icon from library",
      "steps": [
        { "id": "1", "action": "ui.show.icon.picker" },
        { "id": "2", "action": "icon.search" },
        { "id": "3", "action": "icon.select" }
      ],
      "triggers": ["menu.icon.pick"]
    }
  ]
}
