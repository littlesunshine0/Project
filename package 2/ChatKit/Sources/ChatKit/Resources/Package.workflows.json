{
  "workflows": [
    {
      "id": "chat.quick.start",
      "name": "Quick Chat",
      "description": "Start a quick chat session",
      "steps": [
        { "id": "1", "action": "chat.create" },
        { "id": "2", "action": "ui.focus.input" }
      ],
      "triggers": ["shortcut.⌘N", "menu.chat.new"]
    },
    {
      "id": "chat.with.context",
      "name": "Chat with Context",
      "description": "Start chat with file context",
      "steps": [
        { "id": "1", "action": "file.select" },
        { "id": "2", "action": "context.set" },
        { "id": "3", "action": "chat.create" },
        { "id": "4", "action": "ui.focus.input" }
      ],
      "triggers": ["menu.chat.with.context"]
    },
    {
      "id": "chat.export.all",
      "name": "Export All Chats",
      "description": "Export all chat history",
      "steps": [
        { "id": "1", "action": "chat.list" },
        { "id": "2", "action": "select.format" },
        { "id": "3", "action": "chat.export.batch" },
        { "id": "4", "action": "notify.complete" }
      ],
      "triggers": ["menu.chat.export.all"]
    }
  ]
}
