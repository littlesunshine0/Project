{
  "workflows": [
    {
      "id": "bridge.setup",
      "name": "Setup Bridge",
      "description": "Setup a new bridge connection",
      "steps": [
        { "id": "1", "action": "ui.show.bridge.wizard" },
        { "id": "2", "action": "bridge.connect" },
        { "id": "3", "action": "verify.connection" },
        { "id": "4", "action": "notify.complete" }
      ],
      "triggers": ["menu.bridge.setup"]
    }
  ]
}
