{
  "workflows": [
    {
      "id": "system.health.check",
      "name": "Health Check",
      "description": "Run system health check",
      "steps": [
        { "id": "1", "action": "system.memory" },
        { "id": "2", "action": "system.cpu" },
        { "id": "3", "action": "system.disk" },
        { "id": "4", "action": "analyze.health" },
        { "id": "5", "action": "report.status" }
      ],
      "triggers": ["schedule.hourly", "menu.system.health"]
    }
  ]
}
